import { CheckmarckPipe } from './checkmarck.pipe';

describe('CheckmarckPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckmarckPipe();
    expect(pipe).toBeTruthy();
  });
});
